services:
  - type: web
    name: Kod-kubanca
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Brica_final.py --server.port 10000 --server.address 0.0.0.0
    plan: free
